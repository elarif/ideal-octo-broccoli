{
  "name": "@la/web",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate:dry-run": "tsx scripts/migrate-wp-to-pg.ts --dry-run",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
